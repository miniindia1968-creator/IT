ANANT VIJAY VOTING v4 - JPG UPLOAD ROBUST FIX

Changes:
1. Fixed the admin JPG upload request to send FormData directly.
2. The browser is never forced to set multipart Content-Type; it creates the required boundary automatically.
3. JPG/JPEG extension and image/jpeg MIME type are accepted.
4. Server still verifies the actual JPEG file signature (FF D8 FF).
5. Maximum upload size remains 5 MB.
6. Clearer server error when no file reaches the server.
7. Poster preview gets a cache-busting query after upload.
8. Existing Super Admin login, weekly movie editor, voting and results functions are retained.

IMPORTANT:
After replacing the project on Render, deploy/redeploy the service.
After uploading a poster in Super Admin, click "Save Weekly Movies" so the selected poster is saved with that movie.
